<%= form_for(@graph2, html: { class: "form-horizontal", role: "form" }) do |f| %>
  <% if @graph2.errors.any? %>
    <div class="alert alert-danger alert-dismissable" role="alert">
      <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
      <h4><%= pluralize(@graph2.errors.count, "error") %> prohibited this graph2 from being saved:</h4>

      <ul>
      <% @graph2.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :time, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.number_field :time, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :temp, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.number_field :temp, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :humi, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.number_field :humi, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <%= f.submit class: "btn btn-primary" %>
    </div>
  </div>
<% end %>
